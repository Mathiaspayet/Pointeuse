<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.MaPointeuse" parent="android:Theme.Material.Light.NoActionBar" />

    <!-- Splash Screen Theme -->
    <style name="Theme.MaPointeuse.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/purple_500</item>
        <item name="windowSplashScreenAnimatedIcon">@android:drawable/ic_menu_manage</item>
        <item name="postSplashScreenTheme">@style/Theme.MaPointeuse</item>
    </style>
</resources>
